apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'
apply plugin: 'kotlin-kapt'
apply plugin: 'realm-android'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.3"

    defaultConfig {
        applicationId "com.voltek.newsfeed"
        minSdkVersion 21
        targetSdkVersion 25
        versionCode 2
        versionName "1.1"
    }

    buildTypes {

        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }

        debug {
            debuggable true
            testCoverageEnabled true
        }

        buildTypes.each {
            it.buildConfigField 'String', 'ApiKey', ApiKey
        }
    }

    packagingOptions {
        exclude 'META-INF/rxjava.properties'
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    // Instrumentation tests
    androidTestCompile "com.android.support.test.espresso:espresso-core:$versions.espresso", {
        exclude group: 'com.android.support', module: 'support-annotations'
    }
    androidTestCompile "com.android.support.test.espresso:espresso-contrib:$versions.espresso", {
        exclude group: 'com.android.support', module: 'support-annotations'
        exclude group: 'com.android.support', module: 'support-v4'
        exclude group: 'com.android.support', module: 'recyclerview-v7'
        exclude group: 'com.android.support', module: 'appcompat-v7'
        exclude group: 'com.android.support', module: 'design'
    }

    // Unit tests
    testCompile "junit:junit:$versions.junit"
    testCompile "org.mockito:mockito-android:$versions.mockito"
    testCompile "com.squareup.okhttp3:mockwebserver:$versions.mockwebserver", {
        exclude group: 'com.squareup.okio', module: 'okio'
        exclude group: 'com.squareup.okhttp3', module: 'okhttp'
    }
    testCompile "com.nhaarman:mockito-kotlin-kt1.1:$versions.mockitoKotlin", {
        exclude group: 'org.jetbrains.kotlin'
    }

    // Kotlin
    compile "org.jetbrains.kotlin:kotlin-stdlib-jre7:$versions.kotlinGradlePlugin"

    // Android support library
    compile "com.android.support:appcompat-v7:$versions.supportLibrary"
    compile "com.android.support:design:$versions.supportLibrary"
    compile "com.android.support:recyclerview-v7:$versions.supportLibrary"

    // Networking
    compile "com.squareup.retrofit2:retrofit:$versions.retrofit"
    compile "com.squareup.retrofit2:adapter-rxjava2:$versions.retrofit"
    compile "com.squareup.retrofit2:converter-moshi:$versions.retrofit"

    // Rx
    compile "io.reactivex.rxjava2:rxjava:$versions.rxJava"
    compile "io.reactivex.rxjava2:rxandroid:$versions.rxAndroid"
    compile "io.reactivex.rxjava2:rxkotlin:$versions.rxKotlin"
    compile "com.jakewharton.rxbinding2:rxbinding-kotlin:$versions.rxBindingKotlin"
    compile "com.jakewharton.rxbinding2:rxbinding-appcompat-v7-kotlin:$versions.rxBindingKotlin"
    compile "com.jakewharton.rxbinding2:rxbinding-support-v4-kotlin:$versions.rxBindingKotlin"

    // Dependency injection
    compile "com.google.dagger:dagger:$versions.dagger"
    kapt "com.google.dagger:dagger-compiler:$versions.dagger"

    // Images
    compile "com.github.bumptech.glide:glide:$versions.glide"

    // Data storage
    compile "com.orhanobut:hawk:$versions.hawk"
    compile "com.github.vicpinm:krealmextensions:$versions.kotlinRealmExtensions"

    // Parcel
    compile "org.parceler:parceler-api:$versions.parceler"
    kapt "org.parceler:parceler:$versions.parceler"

    // Logging
    compile "com.jakewharton.timber:timber:$versions.timber"

    // Fonts
    compile "uk.co.chrisjenx:calligraphy:$versions.calligraphy"

    // MVP
    compile "com.arello-mobile:moxy:$versions.moxy"
    compile "com.arello-mobile:moxy-app-compat:$versions.moxy"
    kapt "com.arello-mobile:moxy-compiler:$versions.moxy"

    // Animations
    compile "jp.wasabeef:recyclerview-animators:$versions.recyclerviewAnimators"
}
